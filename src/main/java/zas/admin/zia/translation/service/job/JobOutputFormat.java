package zas.admin.zia.translation.service.job;

/**
 * Output format of a translation job. Internal usage only — never exposed in
 * {@link zas.admin.zia.translation.service.dto.TranslationJobResponse}.
 */
public enum JobOutputFormat {
    PDF,
    MARKDOWN
}
